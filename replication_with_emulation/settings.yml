bandwidth_bounds:
  min:
  max:

delay_bounds:
  min:
  max:

uplink_queue_bounds:
  min:
  max:

uplink_loss_bounds:
  min: 0.0
  max: 0.1

replicate_logs:

location:

flows:

run_times: 3

username: ubuntu

schemes:
  - default_tcp
  - vegas
  - bbr
  - ledbat
  - pcc
  - quic
  - scream
  - sprout
  - taova
  - verus
